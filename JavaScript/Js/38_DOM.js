/*DOM:-
. The Docment Object Model is an applicaton programming interface(API) for manipulating HTML and XML document.(add,remove,and modify parts of the documents/HTML)

. With the object model, js gets all the power it needs to created dynamic HTML.

. Js can change all the HTML elements in the page

. Js can change all the HTML attributes in the page

. Js can change all the css styles i the page

. Js can remove existing HTML elements and attributes

. Js can add new HTML elements and attribute

. Js can react to all existing HTML events in the page

. Js can create new HTML events in the page

.NOTE:- In the DOM , all elements ared defined as object. so it will have both property and method 

. The document object represents your web page.

. If you want to access any element in an HTML page. you always start with accessing the document object.*/